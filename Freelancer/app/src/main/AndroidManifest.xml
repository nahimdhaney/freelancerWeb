<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edson.com.freelancer">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logofreelance"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RestablecerPassActivity"
            android:label="@string/title_activity_recuperar_pass"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CodigoVerificacionActivity"
            android:label="@string/title_activity_codigo_verificacion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NuevaPassActivity"
            android:label="@string/title_activity_nueva_pass"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".menuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".VerproductoActivity"></activity>

        <activity android:name=".Activity_List_Estado"></activity>


    </application>

</manifest>